# Next.js App Router example

Copy `.env.example` to `.env.local`, add your Spotify credentials, then run:

```bash
npm install
npm run dev
```

The route is explicitly dynamic. HTTP caching is controlled by the package's
`cacheControl` option rather than Next.js ISR.
